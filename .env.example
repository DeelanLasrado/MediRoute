# MediRoute — Environment template
# Copy to .env and fill in values for docker-compose / local runs

AZURE_OPENAI_ENDPOINT=https://YOUR_RESOURCE.openai.azure.com/
AZURE_OPENAI_API_KEY=your-key-here
AZURE_OPENAI_DEPLOYMENT=gpt-4o

# Azure (production)
# ApplicationInsights__ConnectionString=InstrumentationKey=...
# ConnectionStrings__DefaultConnection=Server=tcp:....database.windows.net,...
# ConnectionStrings__Redis=....redis.cache.windows.net:6380,password=...,ssl=True
